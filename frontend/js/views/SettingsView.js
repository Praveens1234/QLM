import { toast } from '../notifications.js';

export class SettingsView {
    constructor() {
        this.container = document.getElementById('page-settings');
        this.bindEvents();
    }

    bindEvents() {
        // Future non-AI settings events can go here
    }

    async mount() {
        // Future non-AI settings init can go here
    }
}
